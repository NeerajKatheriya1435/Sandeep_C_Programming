#include <stdio.h>
int main(int argc, char const *argv[])
{
    // FILE *ptr = fopen("sample.txt", "r");

    // char str[20];
    // fseek(ptr, -6, SEEK_END);
    // rewind(ptr);
    // long var1 = ftell(ptr);
    // printf("%d", var1);
    // fscanf(ptr, "%s", str);
    // printf("the 1 value is: %s", str);

    return 0;
}
